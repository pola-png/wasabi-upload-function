{
  "name": "wasabi-upload-function",
  "version": "1.0.0",
  "description": "Upload media files to Wasabi storage for XapZap",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "build": "echo 'No build required'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.400.0"
  }
}
